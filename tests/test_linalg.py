import numpy as np
from tropiq.linalg import matvec

# YOUR TASK: write test_matvec_basic() below.
#
# Use the same example as test_matvec.c:
#   A = [[1, 4, 2],
#        [5, 1, 0]]
#   x = [0, 1, 3]
#   expected y = [5.0, 5.0]
#
# Create A and x as NumPy arrays, call matvec(), and assert the result
# matches the expected output. Use np.testing.assert_array_equal() to
# compare arrays — it gives a cleaner error message than == for arrays.

def test_matvec_basic():
    pass
